<li>
  <div class="col-md-4">
    <%= link_to department.name, department %>
  </div>
  <div class="col-md-4">
    <%= link_to 'Edit', edit_admin_department_path(department) %>
    <%= link_to 'Delete', admin_department_path(department), method: :delete,
                                  data: { confirm: "You sure?" } %>
  </div>                                
</li>